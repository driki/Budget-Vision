{
  "name": "Categories",
  "children": [
    <% @categories.each do |root| %>
    {
      "name": "<%=raw root.name%>",
      "size": <%= root.is_expense ? root.expense_budget.to_i : root.revenue_budget.to_i %>
    }<%=@categories.last == root ? "" : ","%>
    <% end %>
  ]
}