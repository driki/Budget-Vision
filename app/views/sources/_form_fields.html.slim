= simple_form_for [@organization, @project, @source], :html => { :class => 'form-horizontal' } do |f|
  - if f.error_notification
    div class="alert alert-error fade in"
      a class="close" data-dismiss="alert" href="#" &times;
      = f.error_notification

  fieldset
    div class="control-group"
      = f.input :title
    div class="control-group"
      = f.input :url
    div class="control-group"
      = f.input :description, :input_html => { :class => "span6", :rows => "4" }
    div class="form-actions"
      = f.button :submit, "Update Source", :class => 'btn btn-primary'
      '  or
      = link_to "Cancel", request.env['HTTP_REFERER'], :class => "btn"